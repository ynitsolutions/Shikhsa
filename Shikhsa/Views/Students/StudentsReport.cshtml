@using Shikhsa.ViewModels.DataFilter


@model StudentReportPageVM

@{
    ViewData["Title"] = "Students Report";
    var cols = Model.Filter.SelectedColumns ??
                 new List<string>();
}

<div class="card">


    <div class="card-body">

        <form method="post" asp-action="StudentsReport">

            <div class="row">

                <div class="col-md-2">
                    <label>@Localizer.Get("Application No")</label>
                    <input asp-for="Filter.ApplicationNo"
                           class="form-control" />
                </div>

                <div class="col-md-2">
                    <label>@Localizer.Get("Student") @Localizer.Get("Name")</label>
                    <input asp-for="Filter.StudentName"
                           class="form-control" />
                </div>

                <div class="col-md-2">
                    <label>@Localizer.Get("Father") @Localizer.Get("Name")</label>
                    <input asp-for="Filter.FatherName"
                           class="form-control" />
                </div>
                <div class="col-md-2">
                    <label>@Localizer.Get("Mother") @Localizer.Get("Name")</label>
                    <input asp-for="Filter.MotherName"
                           class="form-control" />
                </div>
                <div class="col-md-2">
                    <label>@Localizer.Get("Guardian") @Localizer.Get("Name")</label>
                    <input asp-for="Filter.GuardianName"
                           class="form-control" />
                </div>
                <div class="col-md-2">
                    <label>@Localizer.Get("Mobile")</label>
                    <input asp-for="Filter.MobileNo"
                           class="form-control" />
                </div>
                <div class="col-md-2">

                    <label>@Localizer.Get("Class")</label>

                    <select asp-for="Filter.RegClassId"
                            class="form-control select2">

                        <option value="">@Localizer.Get("Select")</option>

                        @foreach (var item in ViewBag.ClassList)
                        {
                            <option value="@item.DataListItemId">
                                @item.DataListItemText
                            </option>
                        }

                    </select>

                </div>
                <div class="col-md-2">

                    <label>@Localizer.Get("Section")</label>

                    <select asp-for="Filter.SectionId"
                            class="form-control select2">

                        <option value="">@Localizer.Get("Select")</option>

                        @foreach (var item in ViewBag.SectionList)
                        {
                            <option value="@item.DataListItemId">
                                @item.DataListItemText
                            </option>
                        }

                    </select>

                </div>
                <div class="col-md-2">

                    <label>@Localizer.Get("Batch")</label>

                    <select asp-for="Filter.AdmissionBatchId"
                            class="form-control select2">

                        <option value="">@Localizer.Get("Select")</option>

                        @foreach (var item in ViewBag.BatchList)
                        {
                            <option value="@item.BatchId">
                                @item.AcademicYear
                            </option>
                        }

                    </select>



                </div>
                <div class="col-md-2">

                    <label>@Localizer.Get("Status")</label>

                    <select asp-for="Filter.StatusId"
                            class="form-control select2">

                        <option value="">@Localizer.Get("Select")</option>

                        @foreach (var item in ViewBag.StatusList)
                        {
                            <option value="@item.DataListItemId">
                                @item.DataListItemText
                            </option>
                        }

                    </select>

                </div>
            </div>

            <div class="card-body">

                <div class="row">

                    <div class="col-md-2">
                        <label>
                            <input type="checkbox"
                                   name="Filter.SelectedColumns"
                                   value="ApplicationNo"
                                   checked="@(Model.Filter.SelectedColumns?.Contains("ApplicationNo") == true)" />
                            @Localizer.Get("Application No")
                        </label>
                    </div>

                    <div class="col-md-2">
                        <label>
                            <input type="checkbox"
                                   name="Filter.SelectedColumns"
                                   value="StudentName"
                                   checked="@(Model.Filter.SelectedColumns?.Contains("StudentName") == true)" />
                            @Localizer.Get("Student") @Localizer.Get("Name")
                        </label>
                    </div>

                    <div class="col-md-2">
                        <label>
                            <input type="checkbox"
                                   name="Filter.SelectedColumns"
                                   value="FatherName"
                                   checked="@(Model.Filter.SelectedColumns?.Contains("FatherName") == true)" />
                            @Localizer.Get("Father") @Localizer.Get("Name")
                        </label>
                    </div>
                    <div class="col-md-2">
                        <label>
                            <input type="checkbox"
                                   name="Filter.SelectedColumns"
                                   value="MotherName"
                                   checked="@(Model.Filter.SelectedColumns?.Contains("MotherName") == true)" />
                              @Localizer.Get("Mother") @Localizer.Get("Name")
                        </label>
                    </div>
                    <div class="col-md-2">
                        <label>
                            <input type="checkbox"
                                   name="Filter.SelectedColumns"
                                   value="GuardianName"
                                   checked="@(Model.Filter.SelectedColumns?.Contains("GuardianName") == true)" />
                            Guardian Name
                        </label>
                    </div>
                    <div class="col-md-2">
                        <label>
                            <input type="checkbox"
                                   name="Filter.SelectedColumns"
                                   value="LocalAddress"
                                   checked="@(Model.Filter.SelectedColumns?.Contains("LocalAddress") == true)" />
                              @Localizer.Get("Local")   @Localizer.Get("Address")
                        </label>
                    </div>
                    <div class="col-md-2">
                        <label>
                            <input type="checkbox"
                                   name="Filter.SelectedColumns"
                                   value="PermanentAddress"
                                   checked="@(Model.Filter.SelectedColumns?.Contains("PermanentAddress") == true)" />
                              @Localizer.Get("Permanent")   @Localizer.Get("Address")
                        </label>
                    </div>

                        
                    <div class="col-md-2">
                        <label>
                            <input type="checkbox"
                                   name="Filter.SelectedColumns"
                                   value="FatherMobile"
                                   checked="@(Model.Filter.SelectedColumns?.Contains("FatherMobile") == true)" />
                              @Localizer.Get("Mobile")
                        </label>
                    </div>

                    <div class="col-md-2">
                        <label>
                            <input type="checkbox"
                                   name="Filter.SelectedColumns"
                                   value="DOB" checked="@(Model.Filter.SelectedColumns?.Contains("DOB") == true)" />
                              @Localizer.Get("DOB")
                        </label>
                    </div>

                    <div class="col-md-2">
                        <label>
                            <input type="checkbox"
                                   name="Filter.SelectedColumns"
                                   value="AadhaarNumber" checked="@(Model.Filter.SelectedColumns?.Contains("AadhaarNumer") == true)" />
                              @Localizer.Get("Aadhaar")
                        </label>
                    </div>

                       
                       
                </div>

            </div>

         

            <br />
         

            <button type="submit" class="border-0 bg-transparent p-0">
                <i class="ti ti-search fs-5"></i>
            </button>

            <button type="submit" formaction="@Url.Action("ExportExcel")" class="border-0 bg-transparent p-0">
                <i class="ti ti-download"></i>
            </button>
           
            <a href="javascript:void(0);" class="btnStatus" id="btnBulkStatus">
                <spam class="ti ti-reload"></spam>
            </a>

        </form>

        <hr />

        <div id="gridContainer">

            <div class="table-responsive">

                <table class="table table-bordered table-striped">

                    <thead>

                        <tr>
                            <th>
                                <input type="checkbox" name="All" value="All" />
                            </th>
                            @if (cols.Contains("ApplicationNo"))
                            {
                                <th>@Localizer.Get("Application No")</th>
                            }

                            @if (cols.Contains("StudentName"))
                            {
                                <th>@Localizer.Get("Student Name")</th>
                            }

                            @if (cols.Contains("FatherName"))
                            {
                                <th>@Localizer.Get("Father") @Localizer.Get("Name")</th>
                            }
                            @if (cols.Contains("MotherName"))
                            {
                                <th>@Localizer.Get("Mother") @Localizer.Get("Name")</th>
                            }
                            @if (cols.Contains("GuardianName"))
                            {
                                <th>@Localizer.Get("Guardian") @Localizer.Get("Name")</th>
                            }
                            @if (cols.Contains("LocalAddress"))
                            {
                                <th>@Localizer.Get("Local") @Localizer.Get("Address")</th>
                            }
                            @if (cols.Contains("PermanentAddress"))
                            {
                                <th>@Localizer.Get("Permanent") @Localizer.Get("Address")</th>
                            }

                            @*  @if (cols.Contains("LocalAddress"))
                {
                    <th>Local Address</th>
                } *@
                            @if (cols.Contains("FatherMobile"))
                            {
                                <th>@Localizer.Get("Mobile")</th>
                            }

                            @if (cols.Contains("DOB"))
                            {
                                <th>@Localizer.Get("DOB")</th>
                            }

                            @if (cols.Contains("AadhaarNumber"))
                            {
                                <th>@Localizer.Get("Aadhaar")</th>
                            }

                            <th width="120">
                                @Localizer.Get("Action")
                            </th>

                        </tr>

                    </thead>

                    <tbody>

                        @if (Model.Students != null &&
                                    Model.Students.Any())
                        {
                            foreach (var item in Model.Students)
                            {
                                <tr>
                                    <td>
                                        <input type="checkbox" name="selectedStudents" value="@item.StudentId" />
                                    </td>
                                    @if (cols.Contains("ApplicationNo"))
                                    {
                                        <td>@item.ApplicationNo</td>
                                    }

                                    @if (cols.Contains("StudentName"))
                                    {
                                        <td>@item.StudentName</td>
                                    }
                                    @if (cols.Contains("FatherName"))
                                    {
                                        <td>@item.FatherName</td>
                                    }
                                    @if (cols.Contains("MotherName"))
                                    {
                                        <td>@item.MotherName</td>
                                    }
                                    @if (cols.Contains("GuardianName"))
                                    {
                                        <td>@item.GuardianName</td>
                                    }
                                    @if (cols.Contains("LocalAddress"))
                                    {
                                        <td>@item.LocalAddress</td>
                                    }
                                    @if (cols.Contains("PermanentAddress"))
                                    {
                                        <td>@item.PermanentAddress</td>
                                    }
                                    @if (cols.Contains("FatherMobile"))
                                    {
                                        <td>@item.FatherMobile</td>
                                    }

                                    @if (cols.Contains("DOB"))
                                    {
                                        <td>
                                            @item.DOB.ToString("dd/MM/yyyy")
                                        </td>
                                    }

                                    @if (cols.Contains("AadhaarNumber"))
                                    {
                                        <td>@item.AadhaarNumber</td>
                                    }

                                    <td>

                                        <permission controller="Students" action="StudentRegistrations" type="edit">
                                            <a asp-action="SaveStudentRegistrations" asp-route-ApplicationNo="@item.ApplicationNo">
                                                <spam class="ti ti-pencil"></spam>
                                            </a>
                                        </permission>
                                        <permission controller="Students" action="Registrations" type="delete">
                                            <a asp-action="DeleteStudentRegistrations" asp-route-id="@item.ApplicationNo">
                                                <spam class="ti ti-trash"></spam>
                                            </a>
                                        </permission>
                                        <permission controller="Students" action="Profile" type="View">
                                            <a asp-action="Profile" asp-route-id="@item.StudentId">
                                                <spam class="ti ti-eye"></spam>
                                            </a>
                                        </permission>
                                        <a href="javascript:void(0);" class="btnStatus" data-studentid="@item.StudentId">
                                            <spam class="ti ti-reload"></spam>
                                        </a>
                                        @*    <button type="button" class="btn btn-primary btnStatus" data-studentid="@item.StudentId">
                                Change Status
                            </button> *@
                                    </td>

                                </tr>
                            }
                        }
                        else
                        {
                            <tr>

                                <td colspan="20"
                                    class="text-center">

                                    @Localizer.Get("No Records Found")

                                </td>

                            </tr>
                        }

                    </tbody>

                </table>

            </div>
            <!--Model PopUP-->
            <div class="modal fade"
                 id="statusModal"
                 tabindex="-1">

                <div class="modal-dialog">

                    <div class="modal-content">

                        <div class="modal-header">

                            <h5 class="modal-title">
                               @Localizer.Get("Update Student Status")
                            </h5>

                            <button type="button"
                                    class="btn-close"
                                    data-bs-dismiss="modal">
                            </button>

                        </div>

                        <div class="modal-body">

                            <input type="hidden"
                                   id="StudentIds" />

                            <div class="mb-3">

                                <label>  @Localizer.Get("Status")</label>

                                <select id="StatusId" class="form-select ">

                                    <option value="">  @Localizer.Get("Select")</option>
                                    @foreach (var item in ViewBag.StatusList)
                                    {
                                        <option value="@item.DataListItemId">
                                            @item.DataListItemText
                                        </option>
                                    }
                                </select>

                            </div>

                            <div id="admissionArea"
                                 style="display:none;">

                                <div class="mb-3">

                                    <label>  @Localizer.Get("Class")</label>

                                    <select id="ClassId" class="form-select ">
                                        <option value="">  @Localizer.Get("Select")</option>
                                        @foreach (var item in ViewBag.ClassList)
                                        {
                                            <option value="@item.DataListItemId">
                                                @item.DataListItemText
                                            </option>
                                        }
                                    </select>

                                </div>

                                <div class="mb-3">

                                    <label>  @Localizer.Get("Section")</label>

                                    <select id="SectionId" class="form-select">
                                        <option value="">  @Localizer.Get("Select")</option>
                                        @foreach (var item in ViewBag.SectionList)
                                        {
                                            <option value="@item.DataListItemId">
                                                @item.DataListItemText
                                            </option>
                                        }
                                    </select>

                                </div>

                                <div class="mb-3">

                                    <label>  @Localizer.Get("Batch")</label>

                                    <select id="BatchId" class="form-select ">
                                        <option value="">@Localizer.Get("Select")</option>

                                        @foreach (var item in ViewBag.BatchList)
                                        {
                                            <option value="@item.BatchId">
                                                @item.AcademicYear
                                            </option>
                                        }
                                    </select>

                                </div>

                            </div>

                        </div>

                        <div class="modal-footer">

                            <button type="button"
                                    class="btn btn-success"
                                    id="btnSaveStatus">
                                  @Localizer.Get("Save")
                            </button>

                        </div>

                    </div>

                </div>

            </div>

        </div>

    </div>

</div>
@section Scripts
{
    <script>
        $("#StatusId").change(function() {
            debugger
            var statusId = $(this).val();
            var modal = new bootstrap.Modal(document.getElementById('admissionArea')
            );
            if (statusId == "27") // Admitted
            {
                // $("#admissionArea").show();
                modal.show();
            }
            else {
                modal.hide();
                // $("#admissionArea").hide();
            }
        });

        $(".btnStatus").click(function() {

            var studentId = $(this).data("studentid");

            $("#StudentIds").val(studentId);
            var modal = new bootstrap.Modal(document.getElementById('statusModal')
            );
            //$("#statusModal").modal("show");
            modal.show();
        });
        $("#btnBulkStatus").click(function() {

            var ids = [];

            $("input[name='selectedStudents']:checked")
                .each(function() {

                    ids.push($(this).val());

                });

            if (ids.length == 0) {
                alert("Select student first");
                return;
            }

            $("#StudentIds").val(ids.join(','));
            var modal =
                new bootstrap.Modal(
                    document.getElementById('statusModal')
                );
            modal.show();

        });
        $("#btnSaveStatus").click(function() {

            $.ajax({
                url: '/Students/UpdateStudentStatus',
                type: 'POST',
                data: {
                    StudentIds: $("#StudentIds").val(),
                    StatusId: $("#StatusId").val(),
                    ClassId: $("#ClassId").val(),
                    SectionId: $("#SectionId").val(),
                    BatchId: $("#BatchId").val()
                },
                success: function(res) {

                    if (res.success) {
                        alert("Saved Successfully!")
                        // Swal.fire(
                        //     'Success',
                        //     res.message,
                        //     'success'
                        // ).then(() => {

                        //     location.reload();

                        // });
                    }
                    else {
                        alert(res.message);
                        // Swal.fire(
                        //     'Error',
                        //     res.message,
                        //     'error');
                    }
                }
            });

        });
    </script>
}
